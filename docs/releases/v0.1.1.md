# Release v0.1.1: Fix Abstürze in Desktop-Verwaltung

**Datum:** 15.01.2026
**Tag:** `v0.1.1`

## 📝 Zusammenfassung
Dieses Release behebt kritische Stabilitätsprobleme im "Desktops verwalten"-Menü, die zum Absturz oder unerwarteten Schließen der Anwendung führten. Zudem wurde das Logging-System im Kernbereich verbessert.

## 🐛 Bugfixes

### GUI / Benutzererfahrung
*   **Fix für schließende Fenster:** Ein Fehler im `ManageDesktopsWindow` wurde behoben, bei dem das Fenster unerwartet geschlossen wurde, wenn ein Dialog (z. B. "Umbenennen", "Hintergrund wählen" oder das Kontextmenü) geöffnet wurde. Das `WindowDeactivate`-Event wird nun während dieser Interaktionen korrekt unterdrückt.

### Core / Services
*   **Keine CLI-Prompts mehr:** Die Funktion `delete_desktop` fordert nun keine Benutzereingabe (`input()`) mehr an, wenn sie aus der GUI aufgerufen wird. Dies verhinderte Abstürze in Umgebungen ohne Konsole.
*   **Stdout-Schutz:** Es wurde ein globaler Schutzmechanismus (`config.py`) implementiert, der `sys.stdout` und `sys.stderr` auf `nul` (devnull) umleitet, falls keine Konsole verfügbar ist (z. B. bei Ausführung als `.pyw` oder Tray-App). Dies verhindert `OSError`-Abstürze bei `print()`-Aufrufen.

## 🛠️ Technische Änderungen
*   **Refactoring:** Der `desktop_service` wurde überarbeitet und nutzt nun konsequent das projektweite `logging`-System anstelle von `print()`-Anweisungen. Dies verbessert die Fehlerdiagnose und verhindert I/O-Fehler.
